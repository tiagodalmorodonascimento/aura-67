const resetForm=document.querySelector('#resetPasswordForm');const resetMessage=document.querySelector('#resetMessage');const resetIntro=document.querySelector('#resetIntro');let recoveryAuthorized=false;
function unlockReset(){recoveryAuthorized=true;resetIntro.textContent='O link foi validado. Escolha uma senha forte e diferente das anteriores.';resetForm.hidden=false;document.querySelector('#backToLogin').hidden=true;}
document.querySelectorAll('.password-toggle').forEach((button)=>button.addEventListener('click',()=>{const input=button.parentElement.querySelector('input');input.type=input.type==='password'?'text':'password';}));
async function initializeReset(){if(!window.auraSupabase){resetIntro.textContent='A conexão segura não está configurada.';return;}window.auraSupabase.auth.onAuthStateChange((event)=>{if(event==='PASSWORD_RECOVERY')unlockReset();});const hasRecoverySignal=location.hash.includes('type=recovery')||new URLSearchParams(location.search).has('code');const{data}=await window.auraSupabase.auth.getSession();if(hasRecoverySignal&&data.session){unlockReset();return;}setTimeout(()=>{if(!recoveryAuthorized){resetIntro.textContent='Este link é inválido ou expirou. Solicite uma nova troca de senha pelo menu da conta.';document.querySelector('#backToLogin').hidden=false;}},900);}
resetForm.addEventListener('submit',async(event)=>{event.preventDefault();const password=document.querySelector('#resetPassword').value;const confirmation=document.querySelector('#resetPasswordConfirm').value;if(password.length<8){resetMessage.textContent='Use pelo menos 8 caracteres.';return;}if(password!==confirmation){resetMessage.textContent='As senhas não coincidem.';return;}const button=resetForm.querySelector('button[type="submit"]');button.disabled=true;resetMessage.textContent='Atualizando sua senha…';const{error}=await window.auraSupabase.auth.updateUser({password});button.disabled=false;if(error){resetMessage.textContent=error.message;return;}resetMessage.classList.add('success');resetMessage.textContent='Senha atualizada com sucesso. Você já pode continuar com sua conta.';resetForm.hidden=true;const link=document.querySelector('#backToLogin');link.href='app.html';link.textContent='Continuar para minha Aura';link.hidden=false;});initializeReset();
